settings.local.json

# The marker inside a forwarding stub, written by .agents/skills/project-link-skills/scripts/link-skills.sh when symlinks are unavailable.
# The symlinks themselves ARE committed — they are how Claude reaches .agents/skills/.
skills/*/.forwarding-stub
